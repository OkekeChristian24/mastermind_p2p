const { pool } = require("../config/db");

const createNewAd = (data) => {
    return new Promise(async (resolve, reject) => {

        // Custion
    });
}

const editAd = (data) => {
    return new Promise(async (resolve, reject) => {

        // Custion
    });
}

module.exports = {
    createNewAd,
    editAd
}